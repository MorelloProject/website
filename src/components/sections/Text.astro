---
const { style, text_content } = Astro.props;
import * as marked from "marked";
const markdown = marked.parse(text_content.text);
---

<div class:list={[style, "container mx-auto mt-10 px-4"]}>
  <p set:html={markdown} />
</div>
